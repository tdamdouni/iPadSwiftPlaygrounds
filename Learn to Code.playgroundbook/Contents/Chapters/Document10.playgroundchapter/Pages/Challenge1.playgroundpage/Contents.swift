/*:
**Challenge:** Activate and deactivate a portal to collect the gems and toggle the switch.
 
For this challenge, you’ll need to use the portal to move Byte to another location in the puzzle world. But you’ll also need Byte to move *over* the portal to access some of the gems.
 
Practice setting properties by activating and deactivating the portal at the right time. In this puzzle, there’s an [instance](glossary://instance) known as `purplePortal` that you can modify using dot notation.
 
 * callout(Using dot notation):

     `purplePortal.isActive = true`
 
Use the name of the instance, `purplePortal`, followed by a period (`.`) and the name of the [property](glossary://property) you'd like to access (`isActive`).
*/
//#-hidden-code
playgroundPrologue()
//#-end-hidden-code
//#-code-completion(everything, hide)
//#-code-completion(currentmodule, show)
//#-code-completion(identifier, show, isOnOpenSwitch, if, func, for, while, moveForward(), turnLeft(), turnRight(), collectGem(), toggleSwitch(), isOnGem, isOnClosedSwitch, var, ., =, isActive, true, false, purplePortal, isBlocked, true, false, isBlockedLeft, &&, ||, !, isBlockedRight)
//#-editable-code Tap to enter code

//#-end-editable-code
//#-hidden-code
playgroundEpilogue()
//#-end-hidden-code
//#-hidden-code
//
//  Contents.swift
//
//  Copyright (c) 2016 Apple Inc. All Rights Reserved.
//
//#-end-hidden-code
