//#-hidden-code
//
//  Contents.swift
//
//  Copyright (c) 2016 Apple Inc. All Rights Reserved.
//
//#-end-hidden-code
/*:
 **Challenge:** Use the NOT operator to turn left when blocked.
 
Just like in the previous exercise, the puzzle world in this challenge is slightly different each time you run it. Try figuring out how to use the [logical NOT operator (!)](glossary://logical%20NOT%20operator) to solve it.
 
* callout(New Condition!):

     The condition `isBlocked` is either **true** or **false**, giving you a [Boolean](glossary://Boolean) value.\
     If you *can't* move forward one tile in the current direction, `isBlocked` is **true**. If you *can* move forward, `isBlocked` is **false**.
*/
//#-code-completion(everything, hide)
//#-code-completion(currentmodule, show)
//#-code-completion(identifier, show, isOnOpenSwitch, moveForward(), turnLeft(), collectGem(), toggleSwitch(), turnRight(), if, func, for, isOnGem, isOnClosedSwitch, isBlocked, !)
//#-hidden-code
playgroundPrologue()
//#-end-hidden-code
//#-editable-code Tap to enter code
for i in 1 ... 13 {
    
}
//#-end-editable-code



//#-hidden-code
playgroundEpilogue()
//#-end-hidden-code

