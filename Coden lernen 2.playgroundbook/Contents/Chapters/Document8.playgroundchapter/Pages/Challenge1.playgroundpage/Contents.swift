//#-hidden-code
//
//  Contents.swift
//
//  Copyright © 2016,2017 Apple Inc. All rights reserved.
//
//#-end-hidden-code
/*:#localized(key: "FirstProseBlock")
 **Challenge:** Assign a new value to `gemCounter` for each additional gem collected.
 
 You've created a [variable](glossary://variable) and changed its value. In this challenge, you’ll practice that skill by correctly setting the value of the variable as you collect additional gems.
 
Assign `gemCounter` a beginning value of `0`. Move your character to each gem and collect it. Then assign the correct value to `gemCounter` for each additional gem that's collected.
 */
//#-hidden-code
playgroundPrologue()
//#-end-hidden-code
//#-code-completion(everything, hide)
//#-code-completion(currentmodule, show)
//#-code-completion(identifier, show, isOnOpenSwitch, if, func, for, while, moveForward(), turnLeft(), turnRight(), collectGem(), toggleSwitch(), isOnGem, isOnClosedSwitch, var, =, <, >, ==, !=, +=, +, -, isBlocked, true, false, isBlockedLeft, &&, ||, !, isBlockedRight)
var gemCounter = /*#-editable-code yourFuncName*/<#T##value##Int#>/*#-end-editable-code*/
//#-hidden-code
initialGemCount = gemCounter
//#-end-hidden-code
//#-editable-code Tap to enter code

//#-end-editable-code
//#-hidden-code
finalGemCount = gemCounter
playgroundEpilogue()
//#-end-hidden-code

